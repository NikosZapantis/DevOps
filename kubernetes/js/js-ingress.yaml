apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: javascript-ingress
spec:
  rules:
    - host: javascript.example.com #TODO: Change to the real domain
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: javascript
                port:
                  number: 80
